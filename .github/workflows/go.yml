name: Build

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:

  build:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Set up Go
      uses: actions/setup-go@v2
      with:
        go-version: 1.16

    - name: Build
      run: go build -v

    - name: Test
      run: go test -v
    
    - name: 'Upload Artifact'
      uses: actions/upload-artifact@v2
      with:
        name: artifact
        path: samsung_aircon_connector/server
        if-no-files-found: error 
